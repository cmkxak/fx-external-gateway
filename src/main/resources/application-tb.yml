# TB(테스트베드) 프로파일 — 비밀 아닌 값만 커밋.
# api-key / api-secret 은 여기 박지 말고 환경변수(THUNES_API_KEY / THUNES_API_SECRET)로 주입.
thunes:
  base-url: ${THUNES_BASE_URL:https://TB-HOST-CHANGE-ME}   # TODO: 실제 TB 호스트로 교체
